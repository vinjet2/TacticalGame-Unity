using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaseEnemy
{
    
    // Start is called before the first frame update
    void Start()
    {
        //spriteR = gameObject.GetComponent<SpriteRenderer>();
        //_sprite = Resources.Load<Sprite>("Lance_Ennemi");
        //spriteR.sprite = _sprite;
        //GetComponent<SpriteRenderer>().sprite = Resources.Load<Sprite>("Lance_Ennemi");
        //string image = this.UnitName + "_Ennemi";
        //print(image);
        //this._sprite = Resources.Load<Sprite>(image);
        //print(this._sprite);
        //this.spriteR.sprite = _sprite;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

